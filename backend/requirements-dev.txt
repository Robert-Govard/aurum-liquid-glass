# Test-only dependencies — kept separate from requirements.txt so the
# production image never installs a test runner. Install inside the running
# backend container (see backend/tests/README.md) rather than baking these
# into the Docker image.
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
